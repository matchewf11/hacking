local M = {}

function M.setup(opts)
  -- The plugin works purely through Neovim's builtin filetype and syntax features.
  -- This setup function is provided for compatibility with standard Neovim plugin conventions,
  -- and can be used in the future to handle configuration options if needed.
end

return M
